# ============================================
# 轻量级 Requirements - 用于生产部署
# ============================================
# 镜像大小：约 500-800 MB（相比原来的 4-7 GB）
# ============================================

# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
pydantic>=2.5.0
email-validator>=2.1.0
httpx[socks]>=0.23.1

# LLM and AI（使用 API，不需要本地模型）
google-genai>=0.1.0

# 基础图像处理（不需要 torch/transformers）
Pillow>=10.0.0

# TTS（使用云服务）
edge-tts>=6.1.9
elevenlabs>=0.2.26

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Utilities
rich>=13.7.0
aiofiles>=23.2.0
Jinja2>=3.0.0

# Google APIs
google-api-python-client>=2.108.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# ============================================
# 说明：
# ============================================
# 移除的重量级依赖：
# - torch (2-3 GB) - 使用云 API 代替
# - transformers (1-2 GB) - 使用云 API 代替
# - diffusers (500 MB) - 使用云 API 代替
# - openai-whisper (1 GB) - 使用 OpenAI API 代替
# - opencv-python (500 MB) - 使用 ffmpeg 代替
# - moviepy (200 MB) - 使用 ffmpeg 代替
# - librosa (200 MB) - 不需要本地音频处理
#
# 优势：
# 1. 镜像大小减少 80-90%
# 2. 构建时间减少 70-80%
# 3. 部署更快
# 4. 成本更低（Railway/Vercel 按资源计费）
# 5. 更容易维护
#
# 权衡：
# - 可选平台能力需要额外 API keys
# - API 调用有成本（但通常比自托管便宜）
# - 需要网络连接
# ============================================
