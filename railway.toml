# Railway 部署配置文件

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# 使用启动脚本（在 Dockerfile 中定义）
# 不指定 startCommand，让 Dockerfile 的 CMD 生效
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
